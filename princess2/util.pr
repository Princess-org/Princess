// copy string
export def copy_string(str: string) -> *string {
    let res = allocate(string)
    (@res).size = str.size
    (@res).value = allocate((size_of char) * str.size)
    memcopy(str.value, (@res).value, str.size)
    return res
}