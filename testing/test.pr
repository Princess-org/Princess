import a
import b

var pos: a::Point = {10, 20}
var rectangle: Rectangle = { pos, 10, 20 }
var recursive: Recursive

var s: Struct

type Struct = struct {
    a: int
}

type Point = struct {
    x: int
    y: int
}

def test {
    let point: Point = {10, 20}
}

def function(a: int, b: double) -> double { return a * b }
def function(b: double, a: int) -> double { return a * b }
def function -> int { return 10 }

def test2 {
    function(1, 1.5)
    function(1.5, 1)
    function()
}

test()
test2()

let d = a::some_value

add(1, 1)
add(1.5, 1.5)