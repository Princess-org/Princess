import b

export var some_value = 20

export type Point

export type Point = struct {
    x: int
    y: int
}

def some_function {
    var t: b::T
    var p: *Point
    p = t.point
}

some_function()

export type Recursive = struct {
    r: *Recursive
    line: Line
}

export def add(a: int, b: int) -> int {
    return a + b
}
export def add(a: double, b: double) -> double {
    return a + b
}