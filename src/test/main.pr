import getopt
import test::testsuite

let options = [
    getopt::option('f', "--filter", "*", "Filter test cases, wildcards * and ? are allowed")
]

let res = getopt::parse(args, options)
if not res { exit(-1) }

test::testsuite::match_string = options[0].value.str

test::testsuite::run_test_suite()