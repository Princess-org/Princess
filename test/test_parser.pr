import json

print("Loading test test_parser!\n")

def parse(str: &string) -> &Json {
    error(runtime::executable, "\n")
}

def #test main {
    print("foo\n")
    error("bar\n")

    assert env.out() == "foo\n"
    assert env.err() == "bar\n"
}